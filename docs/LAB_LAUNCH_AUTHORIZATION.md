# LAB Launch Authorization

A first-launch authorization binds one active approved governance policy to LAB, chat `-1003510920417`, bot `@GoalVision_AI_Lab_Bot`, the live-78 schema fingerprint, exact champion generation and calibration artifact, reasoning/publication policy versions, real-time forward-test evidence, the allowed single-bet markets, no hard minimum decimal-odds floor, and a single-publication capacity. Correct-score and combo bets are prohibited by this first-launch authorization.

Creation requires `AUTHORIZE_FIRST_GOALVISION_LAB_FORWARD_TEST`. Authorization is immutable, expires within seven days, supports append-only revocation, and derives remaining capacity from consumption events. Only a confirmed successful Telegram delivery consumes capacity; failed or uncertain attempts do not silently mutate a counter.
